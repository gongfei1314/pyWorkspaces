# 1 日内做T

> **版本说明**：本文档依据 `2日内做T_动态保护价.py` 反写，与代码逐条对应。
> 与基础版 `日内做T.py` 的差异：
> 1. 新增 `BREAKEVEN_BUF_EARLY`（平价宽缓冲+1.3%）与 `STOPLOSS_BUF_EARLY`（止损宽缓冲-1.2%）：前两笔交易后的等待期（trade_count < 3）同样启用保护（基础版该阶段无任何保护）；
> 2. 持仓持平停止时间 `STOP_FLAT_TIME` 由 14:11 提前至 13:15；
> 3. 策略激活时间 `START_TIME` 由 09:50 提前至 09:45（静默追踪期缩短为 09:30 ~ 09:45）。

## 1.1 基本信息设置

- 做T的ETF代码：588170.SH
- 底仓持仓：57700股
- 每次做T数量：20000股
- 交易方向：低买高卖
- 参考价格：**昨收价**（前一交易日收盘价），首日回退到开盘价

---

# 2 买卖规则

## 2.1 冲高卖出规则

当股价高于昨收价+3%后，触发监控，记录每次的最高点（使用K线最高值high），不断更新最高点记录，当股价从最高点下滑1.2%后，挂单卖出20000股。

## 2.2 回落买入规则

当股价低于昨收价-3%后，触发监控，记录每次的最低点（使用K线最低值low），不断更新最低点记录，当股价从最低点上涨1.2%后，挂单买入20000股。

## 2.3 卖出后回补规则

当卖出20000股后，记录卖出股价，等待触发条件后追踪谷值，从谷值反弹达到阈值后买入20000股。

### 前3笔（trade_count < 3）

- 触发条件：从卖出价继续下跌 ≥ **2%**（`SELL_CONTINUE`）
- 回补条件：从谷值反弹 ≥ **0.5%**（`REBUY_BOUNCE`）

### 第3笔后（trade_count >= 3）

- 触发条件（满足任一）：
  - 从卖出价下跌 ≥ **1.5%**（`SELL_TRACK_TRIGGER`）
  - 从卖出价下跌 ≥ **0.6%**（`SELL_CONTINUE_ALT`）且卖出后已过 **10分钟**（`CONTINUE_WAIT_BARS = 10`根K线）
- 回补条件：从谷值反弹 ≥ **0.4%**（`REBUY_THRESH`）

### 通用规则

- **死区修复**：触发条件达成与回补检查已解耦（使用`sell_drop_achieved`标志位），一旦达成即永久有效，不会被后续价格波动清除。
- **平价保护**（trade_count >= 3时生效，即第三次交易/第二次卖出后）：如果卖出后股价未跌到触发阈值，反而涨超卖出价+0.3%以上，买回平仓，避免踏空。
- **平价保护**（trade_count < 3时生效，即第一次卖出后）：如果卖出后股价未跌到触发阈值，反而涨超卖出价+1.3%以上，买回平仓，避免踏空。不影响第三次的买入。

## 2.4 买入后再卖规则

当买入20000股后，记录买入股价，等待触发条件后追踪峰值，从峰值回调达到阈值后卖出20000股。

### 前3笔（trade_count < 3）

- 触发条件：从买入价继续上涨 ≥ **2%**（`BUY_CONTINUE`）
- 再卖条件：从峰值回调 ≥ **0.5%**（`RESELL_PULLBACK`）

### 第3笔后（trade_count >= 3）

- 触发条件（满足任一）：
  - 从买入价上涨 ≥ **1.5%**（`BUY_TRACK_TRIGGER`）
  - 从买入价上涨 ≥ **0.6%**（`BUY_CONTINUE_ALT`）且买入后已过 **10分钟**（`CONTINUE_WAIT_BARS = 10`根K线）
- 再卖条件：从峰值回调 ≥ **0.4%**（`RESELL_THRESH`）

### 通用规则

- **死区修复**：触发条件达成与再卖检查已解耦（使用`buy_rise_achieved`标志位），一旦达成即永久有效，不会被后续价格波动清除。
- **止损保护**（trade_count >= 3时生效，即第二次买入后）：如果买入后股价未涨到触发阈值，反而跌破买入价-0.3%以下，卖出止损，避免套牢。
- **止损保护**（trade_count < 3时生效，即第一次买入后）：如果买入后股价未涨到触发阈值，反而跌破买入价-1.2%以下，卖出止损，避免套牢。

## 2.5 尾盘平仓规则

在14:50时，检查持仓数据，如果不是57700股，将今日的买卖差额挂单买卖，恢复和底仓相同的仓位。

## 2.6 持仓持平停止规则

13:15后，如果当前持仓等于底仓（57700股），说明当日做T已全部平仓，停止当日剩余时间的一切交易。

## 2.7 trade_count 阈值总结

```
举例：卖(1) → 买(2) → 卖(3) → 【trade_count=3, 启用新参数】 → 买(4)
      买(1) → 卖(2) → 买(3) → 【trade_count=3, 启用新参数】 → 卖(4)
```

| 条件 | 含义 | 规则2.3/2.4参数 | 保护机制 |
|------|------|----------------|----------|
| trade_count < 3 | 前2笔（寻找第2、3笔） | 旧参数：2%触发, 0.5%动作 | 启用（宽缓冲：平价+1.3% / 止损-1.2%） |
| trade_count >= 3 | 第3笔后（寻找第4笔） | 新参数：1.5%或0.6%+10min触发, 0.4%动作 | 启用（0.3%缓冲） |
| trade_count >= 4 | 达上限 | 停止交易，等待14:50平仓 | — |

---

# 3 执行时间控制

## 3.1 静默追踪期（09:30 ~ 09:45）

- 策略不执行任何交易。
- 静默记录日内最高价（intraday_high）和日内最低价（intraday_low）。
- 目的：避免开盘高开/低开的剧烈波动导致错误交易。

## 3.2 策略激活期（09:45 ~ 13:15）

- 正常执行全部买卖规则。
- 进入追踪状态时，使用已记录的日内极值作为初始峰值/谷值。

## 3.3 持仓持平停止期（13:15 ~ 14:49）

- 如果持仓 = 底仓（57700股），停止当日所有交易。
- 如果持仓 != 底仓，继续执行规则2.3/2.4寻找平仓机会。

## 3.4 尾盘平仓期（14:50 ~ 15:00）

- 停止全部策略逻辑。
- 强制恢复底仓至57700股。

---

# 4 交易次数限制

- 每日最多4笔交易。
- 达到上限后停止交易，等待14:50尾盘平仓。

---

# 5 状态机说明

## 5.1 状态定义

| 状态 | 含义 |
|------|------|
| IDLE | 等待±3%触发，静默记录日内极值 |
| TRACKING_HIGH | 冲高+3%后追踪峰值，等回撤1.2%卖出 |
| TRACKING_LOW | 回落-3%后追踪谷值，等反弹1.2%买入 |
| SOLD_TRACKING_LOW | 已卖出，追踪谷值，等待触发后回补买入 |
| BOUGHT_TRACKING_HIGH | 已买入，追踪峰值，等待触发后再卖卖出 |

## 5.2 状态转换图

```
                      ┌────────────────────────────────────────────┐
                      │                                            │
                      ▼                                            │
  IDLE ──(+3%)──→ TRACKING_HIGH ──(回撤1.2% 卖)──→ SOLD_TRACKING_LOW ──(买)──┐
  │                                            ▲                            │
  │                                            │                            │
  └──(-3%)──→ TRACKING_LOW ──(反弹1.2% 买)──→ BOUGHT_TRACKING_HIGH ──(卖)──┘
                                            ▲                   │
                                            └─────(卖)───────────┘
```

- IDLE离开后当日永不回到IDLE，在SOLD_TRACKING_LOW和BOUGHT_TRACKING_HIGH之间循环。
- 检测到新交易日时状态重置为IDLE，trade_count、峰值/谷值、买卖价、标志位等当日状态全部清零（跨日仅保留底仓标志与last_bar_close用于推算昨收价）。
- 每笔交易需do_buy/do_sell返回True才推进状态，失败则保持原状态。

---

# 6 数据来源说明

## 6.1 参考价格

| 变量 | 说明 |
|------|------|
| `prev_close` | 昨收价，前一交易日最后一根K线的收盘价。每日初始化时从`last_bar_close`获取。用于计算`change_ratio`触发±3% |
| `last_bar_close` | 每根K线收盘价，跨日时传递给`prev_close` |
| `open_price` | 当日开盘价，首日（prev_close=0）时作为回退参考价 |

## 6.2 峰值/谷值数据流

| 阶段 | 使用的数据 | 说明 |
|------|-----------|------|
| IDLE（09:45前） | intraday_high / intraday_low | 静默记录日内极值，不交易 |
| IDLE → TRACKING转换瞬间 | intraday_high / intraday_low | 仅用于初始化peak/trough |
| TRACKING_HIGH | peak_price（动态） | 每根K线用high更新 |
| TRACKING_LOW | trough_price（动态） | 每根K线用low更新 |
| SOLD_TRACKING_LOW | trough_price（动态） | 从卖出点重置，之后动态更新 |
| BOUGHT_TRACKING_HIGH | peak_price（动态） | 从买入点重置，之后动态更新 |

## 6.3 K线计数器

| 变量 | 说明 |
|------|------|
| `bars_since_sell` | 卖出后经过的K线数，每根K线+1，卖出时重置为0。用于规则2.3备选触发（0.6%+10分钟） |
| `bars_since_buy` | 买入后经过的K线数，每根K线+1，买入时重置为0。用于规则2.4备选触发（0.6%+10分钟） |

---

# 7 安全保护机制

## 7.1 交易执行保护

| 机制 | 说明 |
|------|------|
| do_sell返回值检查 | 卖出失败（无持仓）时不推进状态，避免状态与实际持仓不一致 |
| do_buy资金检查 | 买入前检查可用资金，不足时返回False，不推进状态 |
| 持仓key兼容查找 | get_holding_amount()支持多种交易所代码格式匹配 |
| 持仓总量查询 | get_holdings()使用`m_nVolume`（总持仓），非`m_nCanUseVolume`（可用），确保含T+1冻结份额的真实持仓 |

## 7.2 价格保护

| 机制 | 触发条件 | 动作 | 附加限制 |
|------|---------|------|---------|
| 平价保护（紧） | 卖出后股价涨超卖出价+0.3% | 买回平仓，避免踏空 | **仅trade_count >= 3时生效（第三次交易/第二次卖出后）** |
| 平价保护（宽） | 卖出后股价涨超卖出价+1.3% | 买回平仓，避免踏空 | **仅trade_count < 3时生效（第一次卖出后），不影响第三次的买入** |
| 止损保护（紧） | 买入后股价跌破买入价-0.3% | 卖出止损，避免套牢 | **仅trade_count >= 3时生效（第二次买入后）** |
| 止损保护（宽） | 买入后股价跌破买入价-1.2% | 卖出止损，避免套牢 | **仅trade_count < 3时生效（第一次买入后）** |

> **设计原理**：
> - 第一次卖出后（trade_count < 3），启动宽平价保护（+1.3%）避免严重踏空，但阈值较高以免过早介入影响正常做T节奏。
> - 第一次买入后（trade_count < 3），启动宽止损保护（-1.2%）避免套牢，但阈值较高以免过早止损影响正常做T节奏。
> - 第二次卖出/买入后（trade_count >= 3），启动紧保护（±0.3%）防止亏损扩大。

## 7.3 时间保护

| 机制 | 说明 |
|------|------|
| 09:45前不交易 | 静默追踪日内极值，避免开盘高开/低开误触发 |
| 13:15持仓持平停止 | 持仓=底仓时停止当日交易，避免尾盘不必要的交易 |
| 14:50强制平仓 | 无论处于何种状态，恢复底仓至57700股 |

## 7.4 次数保护

| 机制 | 说明 |
|------|------|
| 每日最多4笔交易 | 防止高波动日频繁交易产生大量手续费 |
| 达上限后停止策略 | 等待14:50尾盘平仓处理未平仓位 |

---

# 8 参数配置

## 8.1 基本参数

| 参数 | 值 | 说明 |
|------|-----|------|
| LOTS | 20000 | 每次做T股数 |
| BASE_HOLDING | 57700 | 底仓股数 |
| TARGET_CODE | 588170.SH | ETF代码 |
| accountID | 8890523082 | 交易账号 |

## 8.2 触发参数

| 参数 | 值 | 说明 | 适用阶段 |
|------|-----|------|----------|
| UP_TRIGGER | 0.03 (3%) | 冲高触发阈值（相对昨收价） | IDLE → TRACKING_HIGH |
| DOWN_TRIGGER | 0.03 (3%) | 回落触发阈值（相对昨收价） | IDLE → TRACKING_LOW |
| SELL_PULLBACK | 0.012 (1.2%) | 规则2.1：峰值回撤卖出 | TRACKING_HIGH |
| BUY_BOUNCE | 0.012 (1.2%) | 规则2.2：谷值反弹买入 | TRACKING_LOW |

## 8.3 规则2.3参数（卖出后回补）

| 参数 | 值 | 说明 | 适用阶段 |
|------|-----|------|----------|
| SELL_CONTINUE | 0.02 (2%) | 卖后下跌触发（旧） | 前3笔（trade_count < 3） |
| REBUY_BOUNCE | 0.005 (0.5%) | 谷值反弹回补（旧） | 前3笔（trade_count < 3） |
| SELL_TRACK_TRIGGER | 0.015 (1.5%) | 卖后下跌触发（新） | 第3笔后（trade_count >= 3） |
| SELL_CONTINUE_ALT | 0.006 (0.6%) | 备选触发跌幅 | 第3笔后（trade_count >= 3） |
| REBUY_THRESH | 0.004 (0.4%) | 谷值反弹回补（新） | 第3笔后（trade_count >= 3） |

## 8.4 规则2.4参数（买入后再卖）

| 参数 | 值 | 说明 | 适用阶段 |
|------|-----|------|----------|
| BUY_CONTINUE | 0.02 (2%) | 买后上涨触发（旧） | 前3笔（trade_count < 3） |
| RESELL_PULLBACK | 0.005 (0.5%) | 峰值回调再卖（旧） | 前3笔（trade_count < 3） |
| BUY_TRACK_TRIGGER | 0.015 (1.5%) | 买后上涨触发（新） | 第3笔后（trade_count >= 3） |
| BUY_CONTINUE_ALT | 0.006 (0.6%) | 备选触发涨幅 | 第3笔后（trade_count >= 3） |
| RESELL_THRESH | 0.004 (0.4%) | 峰值回调再卖（新） | 第3笔后（trade_count >= 3） |

## 8.5 通用参数

| 参数 | 值 | 说明 |
|------|-----|------|
| CONTINUE_WAIT_BARS | 10 | 备选触发需等待的K线数（10分钟） |
| PROTECTION_BUF | 0.003 (0.3%) | 止损/平价保护缓冲（仅trade_count >= 3） |
| BREAKEVEN_BUF_EARLY | 0.013 (1.3%) | 平价保护宽缓冲（trade_count < 3，第一次卖出后） |
| STOPLOSS_BUF_EARLY | 0.012 (1.2%) | 止损保护宽缓冲（trade_count < 3，第一次买入后） |
| MAX_TRADES | 4 | 每日最大交易笔数 |

## 8.6 时间参数

| 参数 | 值 | 说明 |
|------|-----|------|
| START_TIME | 945 (09:45) | 策略激活时间 |
| STOP_FLAT_TIME | 1315 (13:15) | 持仓=底仓时停止交易 |
| REBALANCE_TIME | 1450 (14:50) | 尾盘平仓时间 |

---

# 9 场景验证：高开+3%后回落到-1%

## 场景假设

```
prev_close = 1.000 (昨收价)
09:35: price = 1.030 (+3%)
09:42: price = 1.045 (+4.5%)
09:50: price = 0.990 (-1%)
```

## 执行追踪

| 时间 | 价格 | can_trade | 状态 | intraday_high | 动作 |
|------|------|-----------|------|---------------|------|
| 09:35 | 1.030 | False | IDLE | 1.030 | 静默记录，不进入TRACKING_HIGH |
| 09:42 | 1.045 | False | IDLE | 1.045 | 静默更新日内最高 |
| 09:50 | 0.990 | True | IDLE | 1.045 | change=-1%，不满足±3%，保持IDLE，不交易 |

结论：不会在-1%的位置触发卖出。

## 对比：09:50仍在+3.5%

| 时间 | 价格 | can_trade | 状态 | 动作 |
|------|------|-----------|------|------|
| 09:50 | 1.035 (+3.5%) | True | IDLE→TRACKING_HIGH | peak=max(1.045,1.036)=1.045，用准确峰值 |
| 09:51 | 1.030 (+3%) | True | TRACKING_HIGH | pullback=(1.045-1.030)/1.045=1.43%>1.2% → 卖出 |

结论：09:50仍在+3%以上时才进入追踪，并使用早盘记录的准确峰值1.045。

---

# 10 死区修复（Dead Zone Fix）

## 10.1 问题描述

原始代码中，规则2.3/2.4的触发条件与动作条件耦合在同一个`if/elif`结构中，产生"死区"：

```
原始逻辑（有Bug）:
if drop_from_sell >= 2%:          # 条件A
    if bounce_from_low >= 0.5%:   # 条件B
        买入
elif breakeven_protection:        # 条件C（与A互斥）
    买入

问题：当股价先跌2%（A成立），然后反弹到不足2%时回落
     → A不再成立 → 进入elif检查保护
     → 0.5%反弹窗口被跳过（死区）
```

## 10.2 修复方案

引入`sell_drop_achieved`和`buy_rise_achieved`标志位，解耦触发达成与动作检查：

```
修复后逻辑:
Phase 1: 检查触发条件是否达成 → 标记flag（永久，直到状态转换重置）
Phase 2a: flag已标记 → 检查反弹/回调 → 买入/卖出
Phase 2b: flag未标记 → 检查保护机制（trade_count >= 3）
```

| 标志位 | 作用 | 重置时机 |
|--------|------|---------|
| `sell_drop_achieved` | 规则2.3：触发条件已达成 | 每次状态转换（买入/卖出后） |
| `buy_rise_achieved` | 规则2.4：触发条件已达成 | 每次状态转换（买入/卖出后） |

---

# 11 持仓查询修复（m_nVolume Fix）

## 11.1 问题描述

`get_holdings()`原使用`m_nCanUseVolume`（可用数量），该字段排除T+1冻结份额。ETF当日买入的份额需T+1才能卖出，导致：

```
场景：4笔策略交易后（2买2卖），实际持仓=57700（=底仓），但：
  m_nCanUseVolume = 57700 - 40000(当日买入冻结) = 17700
  → rebalance误判: diff = 17700 - 57700 = -40000
  → 14:50多买40000股 → 总持仓变97700（严重错误）
```

## 11.2 修复方案

将`get_holdings()`中的`m_nCanUseVolume`改为`m_nVolume`（总持仓）：

| 字段 | 含义 | 修复前 | 修复后 |
|------|------|--------|--------|
| `m_nCanUseVolume` | 可用数量（排除T+1冻结） | 使用 | 不再使用 |
| `m_nVolume` | 总持仓数量（含T+1冻结） | 未使用 | **使用** |

> **影响范围**：`get_holding_amount()` → `do_sell()`、`rebalance_position()` 均受益于此修复。
